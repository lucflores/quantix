# Etapa base: Node 20
FROM node:20

WORKDIR /app

# Copiamos los archivos de dependencias primero
COPY package*.json ./

# ⚠️ Fuerza entorno de desarrollo antes de instalar
ENV NODE_ENV=development

# Instalamos dependencias (incluyendo devDependencies)
RUN npm install

# Copiamos el resto del proyecto
COPY . .

# Evitamos que node_modules del host sobreescriba los del contenedor
VOLUME ["/app/node_modules"]

# Exponemos el puerto de Vite
EXPOSE 5173

# Comando por defecto
CMD ["npm", "run", "dev", "--", "--host"]
